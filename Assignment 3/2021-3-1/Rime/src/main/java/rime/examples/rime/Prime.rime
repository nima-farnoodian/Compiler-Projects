proc main([string]: _args_) {
    var int: i = 3
	val int: n = parseInt(_args_[0])

    if (n >= 1)
    {
        print(2)
    }

    var int: count = 2

	while (count <= n)
    {
		var int: c = 2
		
        while (c < i && i % c != 0)
        {
			c = c + 1
        }

        if (c == i)
        {
            print(i)
            count = count + 1
        }
		
		i = i + 1
    }
}
